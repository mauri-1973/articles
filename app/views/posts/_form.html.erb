<div class="row">

  <div class="col-6 col-xl-6 col-lg-6 col-md-6 col-sm-12 col-xs-12">

    <%= form_with(model: post) do |form| %>

      <% if post.errors.any? %>

        <div class="form-group" style="color: red">

          <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

          <ul>

            <% post.errors.each do |error| %>

              <li><%= error.full_message %></li>

            <% end %>

          </ul>

        </div>

      <% end %>

      <div class="form-group">

        <%= form.label "Título", style: "display: block" %>

        <%= form.text_field :title, class:"form-control input-sm" %>

      </div>

      <div class="form-group">

        <%= form.label "Contenido", style: "display: block" %>

        <%= form.text_area :content, class:"form-control input-sm", style:"height:150px !important" %>

      </div>

      <div class="form-group mt-2 mb-2">

        <%= form.submit "Actualizar Post", class:"btn btn-primary btn-sm" %>

      </div>

    <% end %>

  </div 

</div>

